"use client";

import LoginForm from "@/pages/api/auth/LoginForm";

export default function Layout({ children }) {

    return (
      <LoginForm>{ children }</LoginForm>
    );
  

  
}
//Revofeed123